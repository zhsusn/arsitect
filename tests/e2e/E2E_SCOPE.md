# E2E 范围说明：Arsitect SDLC Visualizer MVP

> Generated by e2e-testing skill
> Date: 2026-06-12
> Policy: default
> Target environment: local dev servers (frontend http://localhost:5173, backend http://localhost:8000)

## P0 用户故事覆盖

| FR ID | 用户故事 | 目标页面 | 验证方式 | E2E-ID |
|-------|---------|---------|---------|--------|
| FR-Bypass-001~003 | Tech Lead 在 Gate 阻塞时申请/查询/审批旁路 | `/bypass` | API + 页面加载 | E2E-01 |
| FR-OpenUI-001~005 | 设计师管理 OpenUI 规格 | `/open-ui` | API + 页面加载 | E2E-02 |
| FR-Sketch-001~004 | 设计师管理 Sketch | `/sketches` | API + 页面加载 | E2E-03 |
| FR-Wireframe-001~004 | 设计师管理 Wireframe | `/wireframe` | API + 页面加载 | E2E-04 |
| Binding-001~003 | 维护架构-代码绑定规则 | `/binding` | API + 页面加载 | E2E-05 |
| E2E-Smoke | 全站导航无报错 | 全部侧边栏路由 | 控制台/网络监控 | E2E-06 |

## 关键端点（来自 openapi.yaml）

- `GET /api/v1/health`
- `POST /api/v1/applications`
- `POST /api/v1/projects`
- `POST /api/v1/projects/{id}/bypass-applications`
- `GET /api/v1/projects/{id}/bypass-applications`
- `POST /api/v1/bypass-applications/{id}/approve`
- `POST /api/v1/projects/{id}/open-ui-specs`
- `POST /api/v1/projects/{id}/sketches`
- `POST /api/v1/projects/{id}/wireframes`
- `POST /api/v1/projects/{id}/binding-rules`

## 需要 Mock 的依赖

| 依赖 | 原因 | Mock 方式 |
|------|------|----------|
| OpenUI 本地 Docker 服务 | MVP 未强制安装 Docker；生成服务可能不可用 | `page.route('**/api/v1/projects/*/open-ui-specs/*/generate', ...)` |
| AI 生成接口 | 不稳定/付费/慢 | 仅验证 Spec CRUD，不触发真实生成 |

## 视觉回归目标

- 首次运行生成 baseline，后续 diff 人工确认。
- 本次 default 策略仅对 `/projects` 列表页采集 baseline。

## 反范围（明确不做）

- 不验证 Skill 执行引擎的完整执行流程（集成测试已覆盖）。
- 不验证 C4 DSL 复杂生成逻辑（单元/集成测试覆盖）。
- 不验证 RBAC 权限拦截（MVP 未实现登录）。
- 不验证邮件/Slack/PagerDuty 等第三方通知通道。
