# UAT 用户故事检查清单（增强版）

> Generated by uat-verification skill  
> Date: 2026-06-04  
> Policy: default  
> Scope: Arsitect SDLC Visualizer MVP — Binding / Bypass / OpenUI / Sketch / Wireframe  
> Pre-check: integration-test PASSED (540 passed, 4 skipped, 92.79% coverage)  
> Code-review: DONE (blocking=0, important fixed, 22 passed on re-verify)

---

## 使用说明

1. **勾选列 `[ ]`** 供人工在预览环境执行后手动勾选。  
2. **前端验证**：在预览环境页面中点击、输入、跳转；若前端页面未实现，改用 Swagger UI (`http://localhost:8000/docs`) 或 Postman。  
3. **P0 项**（标记 🔴）为发布阻塞项，**必须全部通过**方可进入 Gate 3 签字。  
4. **P1 项**（标记 🟡）为后续迭代功能，当前迭代仅记录状态，不阻塞发布。  
5. **异常分支**：在正向流程验证完成后，必须逐项模拟异常输入/状态，观察系统反馈。

---

## 模块一：US-003 HITL 旁路审批（DR-017）

### 🔴 P0 正向流程

| 需求编号 | 用户故事 | 前端操作步骤（预期） | API 验证（当前 MVP 可用） | 预期结果 | 异常分支检查 | 集成测试 | UAT |
|----------|----------|---------------------|--------------------------|----------|-------------|----------|-----|
| FR-Bypass-001 | 作为 Tech Lead，我可以在 Gate 阻塞时申请旁路审批 | 1. 进入项目 Gate 审批页  <br>2. 点击「紧急旁路」按钮  <br>3. 选择旁路原因（如「生产环境紧急修复」）  <br>4. 填写执行摘要（≤500 字）  <br>5. 选择期望授权人  <br>6. 点击「提交申请」 | `POST /api/v1/gates/{gate_id}/bypass`  <br>Payload: `{plan_id, stage_id, skill_id, triggered_by, reason, authorizer_token, deadline_hours}` | 返回 200，记录状态为 `PENDING_POST_APPROVAL`，包含 `record_id` | ① 未填原因 → 表单校验提示  <br>② 理由过短（<20 字）→ 返回 422  <br>③ 重复申请（同一 Gate 已存在待授权记录）→ 拦截提示 | TEST-1504 通过 | [ ] |
| FR-Bypass-002 | 作为授权人，我可以查看项目的旁路申请列表 | 1. 进入工作台 / 项目侧边栏「审计日志」→「旁路记录」  <br>2. 查看列表 | `GET /api/v1/projects/{project_id}/bypass-applications` | 返回 200，数组中包含刚提交的申请记录 | ① 无记录 → 展示空状态插画  <br>② 跨项目越权访问 → 403 | TEST-1504 通过 | [ ] |
| FR-Bypass-003 | 作为授权人，我可以审批旁路申请 | 1. 在列表中点击「查看详情并补审」  <br>2. 查看执行时间线与产物清单  <br>3. 填写补审意见（≥20 字）  <br>4. 点击「通过，正常流转」 | `POST /api/v1/bypass-applications/{record_id}/approve`  <br>Payload: `{approved_by}` | 返回 200，记录状态变为 `CLOSED`，项目恢复流转 | ① 审批不存在记录 → 404  <br>② 未填审批人 → 422 | TEST-1504 通过 | [ ] |

### 🔴 P0 异常路径

| 场景 | 操作步骤 | 预期结果 | 集成测试 | UAT |
|------|----------|----------|----------|-----|
| 申请理由过短 | API：提交 `reason="x"`（仅 1 个字符） | 返回 **422**，提示输入长度不足 | TEST-1505 通过 | [ ] |
| 审批不存在的记录 | API：`POST /bypass-applications/no-such-record/approve` | 返回 **404**，提示记录不存在 | TEST-1506 通过 | [ ] |
| 空列表 | API：`GET /projects/{project_id}/bypass-applications`（无记录的项目） | 返回 **200 + `[]`** | TEST-1507 通过 | [ ] |

### 🟡 P1 扩展功能（当前迭代记录状态，不阻塞发布）

| 需求编号 | 功能描述 | 当前实现状态 | 计划迭代 | UAT |
|----------|----------|-------------|----------|-----|
| AC-F-002 | 授权通知 1s 内送达（站内信+邮件） | 未实现 | P1 | N/A |
| AC-F-005 | 24h 补审倒计时与「待补审」任务卡片 | 未实现 | P1 | N/A |
| AC-F-006 | 超时后 5min 内触发多渠道告警 | 未实现 | P1 | N/A |
| AC-F-007 | 项目标记「旁路待补审」，阻塞下游 Gate | 未实现 | P1 | N/A |
| AC-F-009 | 驳回后生成追溯任务并通知项目负责人 | 未实现 | P1 | N/A |
| AC-V-002 | 无 Tech Lead / Security Officer 角色时隐藏「授权」按钮 | 未实现 | P1 | N/A |
| AC-N-001 | 审计记录不可删除/修改（仅管理员归档） | 未实现 | P1 | N/A |

---

## 模块二：US-015 OpenUI 原型（DR-018）

### 🔴 P0 正向流程

| 需求编号 | 用户故事 | 前端操作步骤（预期） | API 验证（当前 MVP 可用） | 预期结果 | 异常分支检查 | 集成测试 | UAT |
|----------|----------|---------------------|--------------------------|----------|-------------|----------|-----|
| FR-OpenUI-001 | 作为设计师，我可以创建 OpenUI 规格 | 1. 进入「原型工作台」Pg_001  <br>2. 点击「新建规格」  <br>3. 填写规格名称（如「Homepage」）  <br>4. 粘贴 DSL JSON（如 `'{"components":[]}'`）  <br>5. 状态选择 **DRAFT**  <br>6. 点击「保存」 | `POST /api/v1/projects/{project_id}/open-ui-specs`  <br>Payload: `{spec_name, dsl_json, status}` | 返回 **201**，包含 `spec_id`；字段值与输入一致 | ① 非法 `status`（如 `INVALID`）→ **400**  <br>② 空 `spec_name` → **422** | TEST-1508 通过 | [ ] |
| FR-OpenUI-002 | 作为设计师，我可以查看规格列表 | 1. 在 Pg_001 左侧或列表页查看 | `GET /api/v1/projects/{project_id}/open-ui-specs` | 返回 **200**，数组中包含刚创建的规格 | ① 无记录 → 展示空状态 | TEST-1508 通过 | [ ] |
| FR-OpenUI-003 | 作为设计师，我可以查看单条规格详情 | 1. 在列表中点击规格名称进入详情 | `GET /api/v1/open-ui-specs/{spec_id}` | 返回 **200**，字段与创建时一致 | ① 不存在 `spec_id` → **404** | TEST-1508 通过 | [ ] |
| FR-OpenUI-004 | 作为设计师，我可以更新规格 | 1. 进入规格详情页  <br>2. 点击「编辑」  <br>3. 修改名称（如「Dashboard」）或状态为 **ACTIVE**  <br>4. 点击「保存」 | `PATCH /api/v1/open-ui-specs/{spec_id}`  <br>Payload: `{spec_name, status}` | 返回 **200**，字段已更新 | ① 非法状态值 → **400** | TEST-1508 通过 | [ ] |
| FR-OpenUI-005 | 作为设计师，我可以删除规格 | 1. 进入规格详情页  <br>2. 点击「删除」  <br>3. 确认删除 | `DELETE /api/v1/open-ui-specs/{spec_id}` | 返回 **204** | ① 删除后再次 `GET` → **404** | TEST-1508 通过 | [ ] |

### 🔴 P0 异常路径

| 场景 | 操作步骤 | 预期结果 | 集成测试 | UAT |
|------|----------|----------|----------|-----|
| 非法 status 枚举 | API：`POST /open-ui-specs`，`status="INVALID"` | 返回 **400** | TEST-1509 通过 | [ ] |
| 空列表 | API：`GET /projects/{project_id}/open-ui-specs`（无记录项目） | 返回 **200 + `[]`** | TEST-1510 通过 | [ ] |

### 🟡 P1 扩展功能

| 需求编号 | 功能描述 | 当前实现状态 | 计划迭代 | UAT |
|----------|----------|-------------|----------|-----|
| AC-1.1 | 基于 C4 Container 图自动组装提示词 | 未实现 | P1 | N/A |
| AC-1.2 | 调用 OpenUI 服务生成 HTML 原型（<10s） | 未实现 | P1 | N/A |
| AC-1.4 | OpenUI 不可用时自动降级为 Wireframe 预览 | 未实现 | P1 | N/A |
| AC-4.2 | 服务状态指示灯（绿/黄/红/灰） | 未实现 | P1 | N/A |
| BTN-003 | 一键启动 OpenUI 本地 Docker 服务 | 未实现 | P1 | N/A |
| AC-5.3 | 降级切换耗时 <1s | 未实现 | P1 | N/A |

---

## 模块三：US-016 Sketch / Wireframe（DR-019）

### 🔴 P0 正向流程 — Sketch

| 需求编号 | 用户故事 | 前端操作步骤（预期） | API 验证（当前 MVP 可用） | 预期结果 | 异常分支检查 | 集成测试 | UAT |
|----------|----------|---------------------|--------------------------|----------|-------------|----------|-----|
| FR-Sketch-001 | 作为设计师，我可以创建 Sketch | 1. 进入 Sketch 管理页  <br>2. 点击「新建 Sketch」  <br>3. 填写名称、图片 URL、标注 JSON  <br>4. 状态选 **DRAFT**  <br>5. 保存 | `POST /api/v1/projects/{project_id}/sketches`  <br>Payload: `{name, image_url, annotations, status}` | 返回 **201**，包含 `sketch_id` | ① 非法 `status` → **400** | TEST-1511 通过 | [ ] |
| FR-Sketch-002 | 作为设计师，我可以查看 Sketch 列表 | 1. 进入 Sketch 管理页查看列表 | `GET /api/v1/projects/{project_id}/sketches` | 返回 **200**，数组中包含记录 | ① 无记录 → 空状态 | TEST-1511 通过 | [ ] |
| FR-Sketch-003 | 作为设计师，我可以更新 Sketch | 1. 进入 Sketch 详情  <br>2. 修改名称或状态为 **ACTIVE**  <br>3. 保存 | `PATCH /api/v1/sketches/{sketch_id}` | 返回 **200**，字段已更新 | — | TEST-1511 通过 | [ ] |
| FR-Sketch-004 | 作为设计师，我可以删除 Sketch | 1. 进入详情页 → 删除 → 确认 | `DELETE /api/v1/sketches/{sketch_id}` | 返回 **204** | ① 删除后 `GET` → **404** | TEST-1511 通过 | [ ] |

### 🔴 P0 正向流程 — Wireframe

| 需求编号 | 用户故事 | 前端操作步骤（预期） | API 验证（当前 MVP 可用） | 预期结果 | 异常分支检查 | 集成测试 | UAT |
|----------|----------|---------------------|--------------------------|----------|-------------|----------|-----|
| FR-Wireframe-001 | 作为设计师，我可以创建 Wireframe | 1. 进入 Wireframe 管理页  <br>2. 点击「新建 Wireframe」  <br>3. 填写名称、nodes JSON、edges JSON  <br>4. 状态选 **DRAFT**  <br>5. 保存 | `POST /api/v1/projects/{project_id}/wireframes`  <br>Payload: `{name, nodes, edges, status}` | 返回 **201**，包含 `wireframe_id` | ① 非法 `status` → **400** | TEST-1514 通过 | [ ] |
| FR-Wireframe-002 | 作为设计师，我可以查看 Wireframe 列表 | 1. 进入 Wireframe 管理页查看列表 | `GET /api/v1/projects/{project_id}/wireframes` | 返回 **200**，数组中包含记录 | ① 无记录 → 空状态 | TEST-1514 通过 | [ ] |
| FR-Wireframe-003 | 作为设计师，我可以更新 Wireframe | 1. 进入 Wireframe 详情  <br>2. 修改名称或状态  <br>3. 保存 | `PATCH /api/v1/wireframes/{wireframe_id}` | 返回 **200**，字段已更新 | — | TEST-1514 通过 | [ ] |
| FR-Wireframe-004 | 作为设计师，我可以删除 Wireframe | 1. 进入详情页 → 删除 → 确认 | `DELETE /api/v1/wireframes/{wireframe_id}` | 返回 **204** | ① 删除后 `GET` → **404** | TEST-1514 通过 | [ ] |

### 🔴 P0 异常路径

| 场景 | 操作步骤 | 预期结果 | 集成测试 | UAT |
|------|----------|----------|----------|-----|
| Sketch 非法 status | API：`POST /sketches`，`status="INVALID"` | 返回 **400** | TEST-1512 通过 | [ ] |
| Sketch 空列表 | API：`GET /projects/{project_id}/sketches` | 返回 **200 + `[]`** | TEST-1513 通过 | [ ] |
| Wireframe 非法 status | API：`POST /wireframes`，`status="INVALID"` | 返回 **400** | TEST-1515 通过 | [ ] |
| Wireframe 空列表 | API：`GET /projects/{project_id}/wireframes` | 返回 **200 + `[]`** | TEST-1516 通过 | [ ] |

### 🟡 P1 扩展功能

| 需求编号 | 功能描述 | 当前实现状态 | 计划迭代 | UAT |
|----------|----------|-------------|----------|-----|
| AC-1 | DomainMapper：C4 DSL → 7 种页面类型识别 | 未实现 | P1 | N/A |
| AC-2 | LayoutPlanner：生成 SVG 线框图坐标 | 未实现 | P1 | N/A |
| AC-3 | NavigationLinker：页面间跳转关系图 | 未实现 | P1 | N/A |
| AC-5 | 标准数据集识别准确率 ≥80% | 未实现 | P1 | N/A |
| F-002 | 手动修正映射结果并重新生成 | 未实现 | P1 | N/A |
| Pg_004 | 跳转关系图页（力导向/层次/环形布局） | 未实现 | P1 | N/A |

---

## 端到端主链路验证（P0 跨模块闭环）

> 以下流程验证各模块在同一项目下的数据隔离与协同能力，**必须全部执行**。

| 步骤 | 操作内容 | 验证方式 | 预期结果 | UAT |
|------|----------|----------|----------|-----|
| 1 | 创建 Application + Project | API / 前端 | Project 创建成功，返回 `project_id` | [ ] |
| 2 | 为 Project 创建 BindingRule（来源→目标字段映射） | API / 前端 | 201 + `rule_id`；LIST 可查询到 | [ ] |
| 3 | 为 Project 创建 OpenUISpec | API / 前端 | 201 + `spec_id`；LIST 可查询到 | [ ] |
| 4 | 为 Project 创建 Sketch | API / 前端 | 201 + `sketch_id`；LIST 可查询到 | [ ] |
| 5 | 为 Project 创建 Wireframe | API / 前端 | 201 + `wireframe_id`；LIST 可查询到 | [ ] |
| 6 | 查询 Project 下各模块列表 | API / 前端 | 各列表均包含刚创建的记录，数据不串扰 | [ ] |
| 7 | 更新各模块记录（名称/状态） | API / 前端 | PATCH 返回 200，字段生效 | [ ] |
| 8 | 删除各模块记录 | API / 前端 | DELETE 返回 204；再次查询返回 404 / `[]` | [ ] |
| 9 | 提交 Bypass 申请 → 查询列表 → 审批通过 | API / 前端 | 闭环完成：申请 `PENDING_POST_APPROVAL` → 审批 `CLOSED` | [ ] |

---

## 通用基础设施检查项

| 检查项 | 验收标准 | 验证方式 | UAT |
|--------|----------|----------|-----|
| CORS 跨域 | 前端 `http://localhost:5173` 可正常调用后端 `http://localhost:8000` API | 浏览器 DevTools Network 观察无 CORS 错误 | [ ] |
| Swagger UI | `http://localhost:8000/docs` 可访问，接口定义与 `openapi.yaml` 一致 | 浏览器打开，核对路径与字段 | [ ] |
| 健康检查 | `GET /api/v1/health` 返回 `200` + `{"status":"ok"}` | curl / 浏览器 | [ ] |
| 数据隔离 | Project A 的记录不会出现在 Project B 的列表中 | 创建两个 Project，交叉查询验证 | [ ] |
| 并发安全 | 同时创建多条记录，ID 唯一且无冲突 | 脚本或并发请求测试 | [ ] |
